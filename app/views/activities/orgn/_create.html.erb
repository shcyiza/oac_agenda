
    <div class="alert-box lists row">
      <%= render partial: 'activities/orgn/partials/torsos', locals: {activity: activity} %>
      <div class="large-6 columns left">
        <%= render partial: 'activities/orgn/partials/create_message', locals: {activity: activity} %> comme vous.
      </div>
      <%= render partial: 'activities/orgn/partials/network_intel', locals: {activity: activity} %>
    </div>
