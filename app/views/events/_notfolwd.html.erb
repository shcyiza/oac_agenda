
<font color="green"><%= truncate_html(event.enom, :length => 30) %></font> <a href="#" data-reveal-id="folevent_<%= event.id %>">
<font color='grey' onmouseover="this.style.color='red';" onmouseout="this.style.color='grey';" title="Cliquez sur le coeur pour suivre l'événement"><i class="fi-link" ></i>
  <% if Folevent.where(event_id: event.id).count > 0 %>
  <sup><%= Folevent.where(event_id: event.id).count %> </sup>
  <% end %>
</font></a>
<div id="folevent_<%= event.id %>" class="reveal-modal small" data-reveal aria-labelledby="modalTitle" aria-hidden="true" role="dialog">
  <b>suivre l'événement <%= event.enom %>?</b><br><br>
  <%if Folevent.where(event_id: event.id).count > 0 %>
   <font color="red"><%= Folevent.where(event_id: event.id).count %></font> personnne(s) suis(suivent) cette événement.<br>
  <% end %>
    <%= render partial: "folevent" %>
    <a class="close-reveal-modal" aria-label="Close">&#215;</a>
</div>
