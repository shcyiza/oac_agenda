
<div class="contain-to-grid sticky">
<nav class="top-bar" data-topbar="" role="navigation" data-options="sticky_on: large">

<div class="top-bar-section">
  <ul class="title-area">
    <li>
      <a href="/"><font color="white"><%= image_tag("logo.png", size: "32x32") %> Ka'agenda</font></a>
    </li>
    <% if user_signed_in? && current_user.pro == true %>
    <li>
      <a href="#" class="left-off-canvas-toggle button"><font color="white" size="3"><i class="fi-indent-more"></i>Pro</font></a>
    </li>
    <% end %>
  </ul>
<ul class="right">
  <% if user_signed_in? %>
        <li><a href="/activities"><font size="3"><i class="fi-link"></i></font> notification</a></li>
        <li class="has-dropdown">
        <a href="#"> <%= current_user.username%> <i class="fi-torso"></i></a>
        <ul class="dropdown">
          <li><%= link_to 'Changer mon compte', edit_user_registration_path %></li>
          <li><%= link_to 'Se déconnecter', destroy_user_session_path, :method=>'delete' %></li>
        </ul>
      </li>
      <% else %>
      <li><%= link_to 'Se connecter', new_user_session_path %></li>
      <li><%= link_to "S'incrire", new_user_registration_path %></li>
      <% end %>
    </ul>
  </div>
</nav>
</div>
